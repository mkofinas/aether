from setuptools import find_packages, setup

setup(
    name='aether',
    version='0.0.1',
    description="Latent Field Discovery in Interacting Dynamical Systems with Neural Fields",
    packages=find_packages(),
)
